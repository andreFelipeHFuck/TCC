import { Component } from '@angular/core';

import { Button } from '@tcc/components/buttons';

@Component({
  selector: 'lib-auth-thumb',
  imports: [Button],
  templateUrl: './auth-thumb.html',
  styleUrl: './auth-thumb.scss',
})
export class AuthThumb {}
