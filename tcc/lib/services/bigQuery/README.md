# bigQuery

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build bigQuery` to build the library.

## Running unit tests

Run `nx test bigQuery` to execute the unit tests via [Vitest](https://vitest.dev/).
